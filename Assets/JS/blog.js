const MainElement=document.querySelector('main')
console.log('blog')

const Build=function(x,y,z){
   //this will build our blog elements

}
const Empty=function(){
    //this will check if the blog post if empty
    function isBlogEmpty(blog) {
      if (!blog || blog.trim() === '') {
      return true;
   } else {
      return false; // Blog is not empty
  }
}
 }
 const RenderBlog=function(){
    //this will render the blog
 
 }
RenderBlog()
